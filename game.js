import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.180.0/build/three.module.js';
const c=document.querySelector('#game'),s=new THREE.Scene();s.background=new THREE.Color(0x080b08);s.fog=new THREE.Fog(0x080b08,18,70);const cam=new THREE.PerspectiveCamera(60,innerWidth/innerHeight,.1,100),r=new THREE.WebGLRenderer({canvas:c,antialias:true});r.setSize(innerWidth,innerHeight);r.setPixelRatio(Math.min(devicePixelRatio,2));r.shadowMap.enabled=true;r.outputColorSpace=THREE.SRGBColorSpace;r.toneMapping=THREE.ACESFilmicToneMapping;r.toneMappingExposure=1.35;s.add(new THREE.HemisphereLight(0xb4c4a5,0x17120e,2));let sun=new THREE.DirectionalLight(0xffd49b,4);sun.position.set(-20,30,15);sun.castShadow=true;sun.shadow.mapSize.set(2048,2048);s.add(sun);let fire=new THREE.PointLight(0xff6726,15,25);fire.position.y=4;s.add(fire);
const M=(x,rough=.7,metal=0)=>new THREE.MeshStandardMaterial({color:x,roughness:rough,metalness:metal});let floor=new THREE.Mesh(new THREE.PlaneGeometry(100,100),M(0x252a21,.95));floor.rotation.x=-Math.PI/2;floor.receiveShadow=true;s.add(floor);let grid=new THREE.GridHelper(100,50,0x62634d,0x303429);grid.material.opacity=.2;grid.material.transparent=true;s.add(grid);
function B(x,y,z,a,b,d,m=M(0x4c4d40)){let q=new THREE.Mesh(new THREE.BoxGeometry(a,b,d),m);q.position.set(x,y,z);q.castShadow=q.receiveShadow=true;s.add(q)}
for(let i=0;i<16;i++){let a=i*Math.PI/8,x=Math.cos(a)*22,z=Math.sin(a)*22;B(x,2.5,z,2.3,5,2.3,M(0x292c26,.9));B(x,5.3,z,3.2,.7,3.2)}for(let i=0;i<28;i++){let a=Math.random()*6.28,d=7+Math.random()*12;B(Math.cos(a)*d,.5,Math.sin(a)*d,1+Math.random()*1.5,1,1+Math.random()*1.5)}
for(let p of [[-7,1,-8],[8,1,-7],[-10,1,7],[10,1,7]]){let q=new THREE.Mesh(new THREE.OctahedronGeometry(1.15),M(0xff732c,.3));q.position.set(...p);s.add(q);let l=new THREE.PointLight(0xff5928,7,10);l.position.set(...p);s.add(l)}
const player=new THREE.Group();player.position.set(0,0,6);s.add(player);let body=new THREE.Mesh(new THREE.CapsuleGeometry(.58,1.1,5,10),M(0x38515d,.6,.3));body.position.y=1;body.castShadow=true;player.add(body);let head=new THREE.Mesh(new THREE.SphereGeometry(.62,16,12),M(0x171c1c));head.scale.y=.8;head.position.y=1.8;head.castShadow=true;player.add(head);let sword=new THREE.Group(),blade=new THREE.Mesh(new THREE.BoxGeometry(.14,2.5,.3),M(0xdce1d7,.15,.85));blade.position.y=1.1;sword.add(blade);let guard=new THREE.Mesh(new THREE.BoxGeometry(.9,.1,.18),M(0xe0a43d,.3,.6));sword.add(guard);sword.position.set(.8,1,0);sword.rotation.z=-.35;player.add(sword);
let enemies=[],parts=[],keys={},mouse=new THREE.Vector2(),hp=100,score=0,wave=1,attack=0,dash=0,playing=false;function spawn(){let a=Math.random()*6.28,d=17+Math.random()*5,e=new THREE.Group();e.position.set(Math.cos(a)*d,0,Math.sin(a)*d);e.userData={hp:3+wave*.5,hit:0};let core=new THREE.Mesh(new THREE.DodecahedronGeometry(.85,1),M(0x762d25,.5,.2));core.position.y=.9;core.castShadow=true;e.add(core);let ring=new THREE.Mesh(new THREE.TorusGeometry(1,.05,6,20),new THREE.MeshBasicMaterial({color:0xff4a25}));ring.rotation.x=Math.PI/2;ring.position.y=.05;e.add(ring);s.add(e);enemies.push(e)}function burst(pos,col=0xffa52e,n=14){for(let i=0;i<n;i++){let p=new THREE.Mesh(new THREE.IcosahedronGeometry(.08,0),new THREE.MeshBasicMaterial({color:col}));p.position.copy(pos);p.userData={v:new THREE.Vector3((Math.random()-.5)*6,Math.random()*6,(Math.random()-.5)*6),life:.7};s.add(p);parts.push(p)}}function hit(){if(!playing||attack)return;attack=.34;sword.rotation.z=-.35;for(let e of [...enemies])if(e.position.distanceTo(player.position)<3.5){e.userData.hp-=2;burst(e.position.clone().add(new THREE.Vector3(0,1,0)),0xff5a2b,18);if(e.userData.hp<=0){score+=100;burst(e.position,0xffc44d,25);s.remove(e);enemies.splice(enemies.indexOf(e),1)}}}function hurt(n){hp=Math.max(0,hp-n);document.querySelector('#damageFlash').style.opacity=.3;setTimeout(()=>document.querySelector('#damageFlash').style.opacity=0,100);if(hp<=0){playing=false;document.querySelector('#messageTitle').textContent='FALLEN';document.querySelector('#messageSub').textContent=`Wave ${wave}  •  ${score} souls`;document.querySelector('#start').textContent='RISE AGAIN';document.querySelector('#message').style.display='grid'}}
function start(){playing=true;hp=100;score=0;wave=1;enemies.forEach(e=>s.remove(e));enemies=[];for(let i=0;i<5;i++)spawn();document.querySelector('#message').style.display='none';document.querySelector('#hud').style.opacity=1}document.querySelector('#start').onclick=start;addEventListener('keydown',e=>{keys[e.code]=1;if(e.code==='ShiftLeft'||e.code==='ShiftRight')if(!dash&&playing){let v=new THREE.Vector3((keys.KeyD?1:0)-(keys.KeyA?1:0),0,(keys.KeyS?1:0)-(keys.KeyW?1:0));if(!v.length())v.z=-1;player.position.addScaledVector(v.normalize(),6);dash=.9;burst(player.position,0xffbd43,18)}});addEventListener('keyup',e=>keys[e.code]=0);addEventListener('mousedown',e=>e.button===0&&hit());addEventListener('mousemove',e=>{mouse.x=e.clientX/innerWidth*2-1;mouse.y=-(e.clientY/innerHeight*2-1)});
const ray=new THREE.Raycaster(),pl=new THREE.Plane(new THREE.Vector3(0,1,0));let wm=new THREE.Vector3(),last=performance.now();function loop(t){let dt=Math.min((t-last)/1000,.033);last=t;if(playing){let v=new THREE.Vector3((keys.KeyD?1:0)-(keys.KeyA?1:0),0,(keys.KeyS?1:0)-(keys.KeyW?1:0));if(v.length())player.position.addScaledVector(v.normalize(),dt*7);player.position.x=THREE.MathUtils.clamp(player.position.x,-19,19);player.position.z=THREE.MathUtils.clamp(player.position.z,-19,19);ray.setFromCamera(mouse,cam);ray.ray.intersectPlane(pl,wm);let a=new THREE.Vector3(wm.x-player.position.x,0,wm.z-player.position.z);if(a.length()>1)player.rotation.y=Math.atan2(a.x,a.z);if(attack){attack-=dt;sword.rotation.z=-.35-Math.sin((.34-attack)/.34*Math.PI)*2;if(attack<=0)sword.rotation.z=-.35}dash=Math.max(0,dash-dt);for(let e of enemies){let v=player.position.clone().sub(e.position);v.y=0;let d=v.length();if(d>1.6){e.position.addScaledVector(v.normalize(),dt*(1.5+wave*.08));e.rotation.y=Math.atan2(v.x,v.z)}else if(e.userData.hit<=0){hurt(7);e.userData.hit=.8}else e.userData.hit-=dt;e.children[0].rotation.y+=dt*2}if(enemies.length===0){wave++;for(let i=0;i<4+wave*2;i++)spawn()}for(let i=parts.length-1;i>=0;i--){let p=parts[i];p.userData.life-=dt;p.position.addScaledVector(p.userData.v,dt);p.userData.v.y-=10*dt;if(p.userData.life<0){s.remove(p);parts.splice(i,1)}}document.querySelector('#healthBar').style.width=hp+'%';document.querySelector('#healthText').textContent=`${Math.ceil(hp)} / 100`;document.querySelector('#score').textContent=score;document.querySelector('#wave').textContent=wave;document.querySelector('#dashBar').style.width=(dash?100:0)+'%'}cam.position.lerp(new THREE.Vector3(player.position.x,8,player.position.z+11),.09);cam.lookAt(player.position.x,0,player.position.z);fire.intensity=14+Math.sin(t*.006)*3;r.render(s,cam);requestAnimationFrame(loop)}requestAnimationFrame(loop);setTimeout(()=>{document.querySelector('#loading').style.opacity=0;setTimeout(()=>document.querySelector('#loading').remove(),700)},900);addEventListener('resize',()=>{cam.aspect=innerWidth/innerHeight;cam.updateProjectionMatrix();r.setSize(innerWidth,innerHeight)})
